<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SplashTheme" parent="android:Theme.Translucent.NoTitleBar.Fullscreen">
        <!--<item name="android:windowBackground">@drawable/splash_background</item>-->
    </style>

    <style name="topMenuBar">
        <item name="tmb_padding">3dp</item>
        <item name="tmb_textSize">18sp</item>
        <item name="tmb_textColor">@android:color/white</item>
        <item name="tmb_backgroundColor">@color/theme</item>
    </style>

    <!-- iOSActionSheet底部Dialog的样式 ，有模糊效果 -->
    <style name="iOSActionSheetDialogStyle" parent="@android:style/Theme.Dialog">

        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 浮于Activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- Dialog以外的区域模糊效果 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- Dialog进入及退出动画 -->
        <item name="android:windowAnimationStyle">@style/iOSActionSheetDialogAnimation</item>
    </style>

    <!-- iOSActionSheet进出动画 -->
    <style name="iOSActionSheetDialogAnimation" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/iosactionsheet_dialog_in</item>
        <item name="android:windowExitAnimation">@anim/iosactionsheet_dialog_out</item>
    </style>


    <!-- 自定义仿IOS的AlertDialog的样式 -->
    <style name="iOSAlertDialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <declare-styleable name="ExpandLinearLayout">
        <attr name="ell_title" format="string" />
        <attr name="ell_arrowSrc" format="reference" />
        <attr name="ell_titleSize" format="dimension" />
        <attr name="ell_itemTextSize" format="dimension" />
        <attr name="ell_titleColor" format="color" />
        <attr name="ell_itemTextColor" format="reference" />
        <attr name="ell_itemTextBackground" format="reference" />
    </declare-styleable>

    <style name="TreasureLinearProgress">
        <item name="lpd_maxLineWidth">80%</item>
        <item name="lpd_minLineWidth">10%</item>
        <item name="lpd_strokeSize">8dp</item>
        <item name="lpd_strokeColor">@color/theme</item>
        <item name="lpd_strokeSecondaryColor">@color/light_gray</item>
        <item name="lpd_reverse">false</item>
        <item name="lpd_travelDuration">1000</item>
        <item name="lpd_transformDuration">400</item>
        <item name="lpd_keepDuration">400</item>
        <item name="lpd_transformInterpolator">@android:anim/decelerate_interpolator</item>
        <item name="pv_progressMode">determinate</item>
        <item name="lpd_inAnimDuration">400</item>
        <item name="lpd_outAnimDuration">400</item>
        <item name="lpd_verticalAlign">center</item>
    </style>

</resources>
